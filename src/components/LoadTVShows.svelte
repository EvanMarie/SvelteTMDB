<script>
	import { fetchTVShows, fetchGenres } from '../lib/fetchFilms';
	// Rest of the imports and variables similar to LoadMovies.svelte
	
	let tvShows = [];

	// Same code as in LoadMovies but replace all instances of movies with tvShows
	// and replace all instances of fetchMovies with fetchTVShows
</script>

<!-- Replace the HTML similar to LoadMovies but replace all instances of movie with tvShow -->
